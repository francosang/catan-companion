CREATE TABLE player (
  id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL
);

CREATE INDEX full_name_full_name ON player(name);

INSERT INTO player (name)
VALUES ('Ryan Getzlaf');


selectAll:
SELECT *
FROM player;

insert:
INSERT INTO player(name)
VALUES (?);
